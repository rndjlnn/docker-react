language: generic

sudo: required
services:
    - docker

before_install:
    - docker build -t rndjlnn/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true rndjlnn/docker-react npm run test -- --coverage
